# yaml-language-server: $schema=../config.schema.json

http:
  apps: 
    - listen:
        - addr: 24200
      return:
        status: 200
        response_headers:
          - [ content-type, text/plain ]
          - [ x-test, yaml-ext ]