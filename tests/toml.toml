[http]
  [[http.apps]]
    server_names = [ { regex = ".*" } ]
    listen = [ { addr = 24100 } ]
    return = { status = 200, response_headers = [ [ "content-type", "text/plain" ], [ "x-test", "toml" ] ] }