pidfile: "%PIDFILE%"

http:
  apps:
    - server_names:
        - regex: .*
      listen:
        - addr: 22700
      return: 
        status: 200
        response_headers:
          - [ content-type, text/plain ]
          - [ x-test, pidfile ]